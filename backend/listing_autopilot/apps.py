"""Rentora AI Listing Autopilot — app config (Phase 19.3)."""

from django.apps import AppConfig


class ListingAutopilotConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "listing_autopilot"
    verbose_name = "AI Listing Autopilot"
