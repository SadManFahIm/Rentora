Hi {{ user.first_name|default:user.username }},

The {{ tier_display }} promotion on "{{ room.title }}" has expired, so the listing has
returned to the standard (Free) tier. It's still fully visible — it just no longer gets the
boosted search position or badge.

Re-promote it anytime from your dashboard to keep the boost going: {{ action_url }}

— The {{ site_name }} Team
